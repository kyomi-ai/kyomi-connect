# Review Log — 2026-05-19

---

### 16:30 — BigQuery dry_run top-level totalBytesProcessed fallback (KYO-26)

**Review type**: initial
**Result**: Clean (approved)
**Files**: `crates/kyomi-datasource/src/providers/bigquery.rs`

No issues found.

**Notes**: The production fix (7 lines) is a textbook extension of the existing `.or_else()` pattern already present in `execute_query` (lines 717-724). The three unit tests inline the same 12-line extraction chain rather than factoring it into a test helper. This is borderline copy-paste but acceptable here: no production helper exists to call, no production code refactoring is in scope for a bug fix, and each test deliberately exhibits the full chain it is exercising. If a third call site is ever added, extracting `fn extract_bytes_processed(v: &Value) -> Option<i64>` as a module-level helper should be done at that point.
