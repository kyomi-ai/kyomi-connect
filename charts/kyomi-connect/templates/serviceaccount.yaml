{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kyomi-connect.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "kyomi-connect.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
